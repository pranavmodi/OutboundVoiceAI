# Backfill backend (appointment + campaign APIs)
NEXT_PUBLIC_BACKFILL_API_URL=http://localhost:8001

# Main operator UI (Cancellation Backfill campaigns tab)
NEXT_PUBLIC_BACKFILL_UI_URL=http://localhost:3000

# Dev server port (default 3001 — see run-frontend-dev.sh)
PORT=3001
